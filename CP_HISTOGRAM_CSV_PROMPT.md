# CP Histogram CSV 解析 Prompt 沉淀

## 使用目的
用于后续快速复用「CP Histogram」工具的 CSV 解析逻辑与需求口径，避免重复沟通。

## 推荐 Prompt 模板
你是一个前端数据可视化解析器，请按以下固定规则解析 Summary CSV 并输出结构化结果：

1. CSV 固定结构：
- 第 23 行（1-based）是字段行。
- 第 29 行是 `UpperLimit`。
- 第 30 行是 `LowerLimit`。
- 第 34 行开始是数据行。

2. 片号与基础字段：
- `Wafer` / `wafer_ID` 表示片号。
- `Total` 表示测量点数（优先从头部元信息读取）。

3. 测试项识别：
- 从第 23 行中自动识别测试项列。
- 测试项列不是写死，需排除固定基础列（No、DIE_X、DIE_Y、PASS/FAIL、SBIN、HBIN、LOT_ID、wafer_ID 等）。

4. spec 处理：
- 每个测试项默认从 CSV 的 `UpperLimit/LowerLimit` 读取 spec。
- 支持用户手动覆盖 spec 值。
- 若 CSV 缺失 spec 且用户未输入，则不画 spec 线，并提示。

5. 绘图与统计：
- 用户选择测试项后，按 wafer 维度「一图一卡」生成直方图。
- 横轴 = 测试值，纵轴 = 数量。
- 直方图自动分箱（自动 bins）。
- 统计包含：测量点数（Total）、数量、平均数、总体方差（除以 N）。
- 不需要合计行。

6. 筛选规则：
- 提供用户输入的“下限/上限”用于筛选绘图与统计数据。
- 上下限必须同时填写才生效。
- 若未填写，默认使用文件全量数据。
- 范围外数据剔除，不参与数量/均值/方差计算。

7. x-min / x-max 显示规则：
- 显示当前筛选后数据的最小值/最大值。

8. 交互规则：
- 允许一次上传多个 CSV。
- 同名文件禁止上传，并提示用户。
- 文件选择区域支持“全选”按钮。
- 页面切换后返回，当前会话内状态需保留（重启应用后可清空）。

请输出：
- 测试项列表
- 每片 wafer 的数据数组
- 每个测试项的默认 spec
- 每片 wafer 的统计结果

## 当前需求理解（实施基线）
- 新工具名称：`CP Histogram`
- 分类：`CP工具`
- 路由：`/tools/cp-histogram`
- 数据展示：每个 wafer 一张直方图卡片
- spec 线来源：CSV 默认 + 用户可覆盖
- 切换工具后保留状态：是（仅当前会话）
- 文档格式：`md`

## 实施注意事项
- CSV 中测试项列是动态的，不可硬编码列名。
- 行号采用 1-based 口径对齐业务说明，代码内部可转为 0-based。
- 对于空值、非法数字、缺失列，需做容错并输出可读错误信息。
